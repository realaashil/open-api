/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetConfigurationUpdateToolsetConfig } from "../../funcs/toolsetConfigurationUpdateToolsetConfig.js";
import { UpdateToolsetConfigRequest$zodSchema } from "../../models/updatetoolsetconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateToolsetConfigRequest$zodSchema,
};

export const tool$toolsetConfigurationUpdateToolsetConfig: ToolDefinition<
  typeof args
> = {
  name: "toolset-configuration-update-toolset-config",
  description: `Update toolset configuration

Update toolset configuration`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await toolsetConfigurationUpdateToolsetConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
