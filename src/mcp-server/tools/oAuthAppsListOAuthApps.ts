/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthAppsListOAuthApps } from "../../funcs/oAuthAppsListOAuthApps.js";
import { ListOAuthAppsRequest$zodSchema } from "../../models/listoauthappsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListOAuthAppsRequest$zodSchema.optional(),
};

export const tool$oAuthAppsListOAuthApps: ToolDefinition<typeof args> = {
  name: "O-auth-apps-list-O-auth-apps",
  description: `List OAuth apps

List all OAuth apps registered for the organization.
<br><br>
Returns a paginated list of apps with their configuration (excluding secrets).
<br><br>
<b>Filters:</b><br>
- \`status\` - Filter by app status (active, suspended, revoked)<br>
- \`search\` - Search by app name
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await oAuthAppsListOAuthApps(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
