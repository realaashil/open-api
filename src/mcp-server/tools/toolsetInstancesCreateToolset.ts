/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetInstancesCreateToolset } from "../../funcs/toolsetInstancesCreateToolset.js";
import { CreateToolsetRequest$zodSchema } from "../../models/createtoolsetop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateToolsetRequest$zodSchema,
};

export const tool$toolsetInstancesCreateToolset: ToolDefinition<typeof args> = {
  name: "toolset-instances-create-toolset",
  description: `Create toolset instance

Create a new toolset instance with authentication configuration
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await toolsetInstancesCreateToolset(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
