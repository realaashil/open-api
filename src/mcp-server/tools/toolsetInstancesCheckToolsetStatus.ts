/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetInstancesCheckToolsetStatus } from "../../funcs/toolsetInstancesCheckToolsetStatus.js";
import { CheckToolsetStatusRequest$zodSchema } from "../../models/checktoolsetstatusop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CheckToolsetStatusRequest$zodSchema,
};

export const tool$toolsetInstancesCheckToolsetStatus: ToolDefinition<
  typeof args
> = {
  name: "toolset-instances-check-toolset-status",
  description: `Check toolset status

Check authentication status of a toolset instance`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await toolsetInstancesCheckToolsetStatus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
