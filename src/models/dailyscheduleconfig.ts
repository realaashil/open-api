/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const DailyScheduleConfigScheduleType = {
  Daily: "daily",
} as const;
export type DailyScheduleConfigScheduleType = ClosedEnum<
  typeof DailyScheduleConfigScheduleType
>;

export const DailyScheduleConfigScheduleType$zodSchema = z.enum([
  "daily",
]);

/**
 * Run crawling job once per day at a specified time
 */
export type DailyScheduleConfig = {
  scheduleType: DailyScheduleConfigScheduleType;
  isEnabled?: boolean | undefined;
  timezone?: string | undefined;
  hour: number;
  minute: number;
};

export const DailyScheduleConfig$zodSchema: z.ZodType<DailyScheduleConfig> = z
  .object({
    hour: z.int(),
    isEnabled: z.boolean().default(true),
    minute: z.int(),
    scheduleType: DailyScheduleConfigScheduleType$zodSchema,
    timezone: z.string().default("UTC"),
  }).describe("Run crawling job once per day at a specified time");
