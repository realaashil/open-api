/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthAppsCreateOAuthApp } from "../../funcs/oAuthAppsCreateOAuthApp.js";
import { CreateOAuthAppRequest$zodSchema } from "../../models/createoauthapprequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateOAuthAppRequest$zodSchema,
};

export const tool$oAuthAppsCreateOAuthApp: ToolDefinition<typeof args> = {
  name: "O-auth-apps-create-O-auth-app",
  description: `Create OAuth app

Create a new OAuth app for the organization.
<br><br>
<b>Important:</b> The client secret is only returned once during creation.
Store it securely - it cannot be retrieved later. If lost, you'll need to
regenerate it.
<br><br>
<b>Admin Only:</b> Requires admin privileges.
<br><br>
<b>Rate Limited:</b> 10 requests per minute.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await oAuthAppsCreateOAuthApp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
