/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userAccountForgotPassword } from "../../funcs/userAccountForgotPassword.js";
import { ForgotPasswordRequest$zodSchema } from "../../models/forgotpasswordrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ForgotPasswordRequest$zodSchema,
};

export const tool$userAccountForgotPassword: ToolDefinition<typeof args> = {
  name: "user-account-forgot-password",
  description: `Request password reset email

Send a password reset link to the user's email.
The link contains a time-limited token that can be used to reset the password.
<br><br>
<b>Note:</b> This endpoint always returns 200 even if the email doesn't exist (to prevent email enumeration).
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await userAccountForgotPassword(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
