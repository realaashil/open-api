/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ScheduleConfig, ScheduleConfig$zodSchema } from "./scheduleconfig.js";

/**
 * Data payload stored with each crawling job in the queue
 */
export type CrawlingJobData = {
  connector: string;
  connectorId: string;
  scheduleConfig: ScheduleConfig;
  orgId: string;
  userId: string;
  timestamp: string;
  metadata?: { [k: string]: any } | undefined;
};

export const CrawlingJobData$zodSchema: z.ZodType<CrawlingJobData> = z.object({
  connector: z.string(),
  connectorId: z.string(),
  metadata: z.record(z.string(), z.any()).optional(),
  orgId: z.string(),
  scheduleConfig: ScheduleConfig$zodSchema,
  timestamp: z.iso.datetime({ offset: true }),
  userId: z.string(),
}).describe("Data payload stored with each crawling job in the queue");
