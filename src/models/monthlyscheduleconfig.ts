/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const MonthlyScheduleConfigScheduleType = {
  Monthly: "monthly",
} as const;
export type MonthlyScheduleConfigScheduleType = ClosedEnum<
  typeof MonthlyScheduleConfigScheduleType
>;

export const MonthlyScheduleConfigScheduleType$zodSchema = z.enum([
  "monthly",
]);

/**
 * Run crawling job on a specified day of each month
 */
export type MonthlyScheduleConfig = {
  scheduleType: MonthlyScheduleConfigScheduleType;
  isEnabled?: boolean | undefined;
  timezone?: string | undefined;
  dayOfMonth: number;
  hour: number;
  minute: number;
};

export const MonthlyScheduleConfig$zodSchema: z.ZodType<MonthlyScheduleConfig> =
  z.object({
    dayOfMonth: z.int(),
    hour: z.int(),
    isEnabled: z.boolean().default(true),
    minute: z.int(),
    scheduleType: MonthlyScheduleConfigScheduleType$zodSchema,
    timezone: z.string().default("UTC"),
  }).describe("Run crawling job on a specified day of each month");
