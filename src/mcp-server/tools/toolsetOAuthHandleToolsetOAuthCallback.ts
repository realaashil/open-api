/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetOAuthHandleToolsetOAuthCallback } from "../../funcs/toolsetOAuthHandleToolsetOAuthCallback.js";
import { HandleToolsetOAuthCallbackRequest$zodSchema } from "../../models/handletoolsetoauthcallbackop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: HandleToolsetOAuthCallbackRequest$zodSchema.optional(),
};

export const tool$toolsetOAuthHandleToolsetOAuthCallback: ToolDefinition<
  typeof args
> = {
  name: "toolset-O-auth-handle-toolset-O-auth-callback",
  description: `Handle OAuth callback

Handle OAuth callback from the authorization server.<br><br>
This endpoint processes the authorization code and completes the OAuth flow.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await toolsetOAuthHandleToolsetOAuthCallback(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
