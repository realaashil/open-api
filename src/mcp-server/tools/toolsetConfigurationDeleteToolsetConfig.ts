/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetConfigurationDeleteToolsetConfig } from "../../funcs/toolsetConfigurationDeleteToolsetConfig.js";
import { DeleteToolsetConfigRequest$zodSchema } from "../../models/deletetoolsetconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteToolsetConfigRequest$zodSchema,
};

export const tool$toolsetConfigurationDeleteToolsetConfig: ToolDefinition<
  typeof args
> = {
  name: "toolset-configuration-delete-toolset-config",
  description: `Delete toolset configuration

Delete a toolset instance and its configuration`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await toolsetConfigurationDeleteToolsetConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
