/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthAppsUpdateOAuthApp } from "../../funcs/oAuthAppsUpdateOAuthApp.js";
import { UpdateOAuthAppRequestRequest$zodSchema } from "../../models/updateoauthappop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateOAuthAppRequestRequest$zodSchema,
};

export const tool$oAuthAppsUpdateOAuthApp: ToolDefinition<typeof args> = {
  name: "O-auth-apps-update-O-auth-app",
  description: `Update OAuth app

Update an OAuth app's configuration.
<br><br>
<b>Admin Only:</b> Requires admin privileges.
<br><br>
<b>Rate Limited:</b> 10 requests per minute.
<br><br>
<b>Note:</b> To regenerate the client secret, use the
\`/oauth-clients/{appId}/regenerate-secret\` endpoint.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await oAuthAppsUpdateOAuthApp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
