/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetConfigurationGetToolsetConfig } from "../../funcs/toolsetConfigurationGetToolsetConfig.js";
import { GetToolsetConfigRequest$zodSchema } from "../../models/gettoolsetconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetToolsetConfigRequest$zodSchema,
};

export const tool$toolsetConfigurationGetToolsetConfig: ToolDefinition<
  typeof args
> = {
  name: "toolset-configuration-get-toolset-config",
  description: `Get toolset configuration

Get configuration for a specific toolset instance`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await toolsetConfigurationGetToolsetConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
