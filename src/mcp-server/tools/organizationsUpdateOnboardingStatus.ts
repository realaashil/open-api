/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsUpdateOnboardingStatus } from "../../funcs/organizationsUpdateOnboardingStatus.js";
import { UpdateOnboardingStatusRequest$zodSchema } from "../../models/updateonboardingstatusop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateOnboardingStatusRequest$zodSchema,
};

export const tool$organizationsUpdateOnboardingStatus: ToolDefinition<
  typeof args
> = {
  name: "organizations-update-onboarding-status",
  description: `Update onboarding status

Update the organization's onboarding progress.<br><br>
<b>Use Cases:</b><br>
<ul>
<li>Mark a step as completed</li>
<li>Skip optional steps</li>
<li>Complete entire onboarding</li>
</ul>
<b>Behavior:</b><br>
<ul>
<li>Steps must be completed in order (unless skippable)</li>
<li>Completing all required steps marks onboarding as complete</li>
</ul>
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await organizationsUpdateOnboardingStatus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
