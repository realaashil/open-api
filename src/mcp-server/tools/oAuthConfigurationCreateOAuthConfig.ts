/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthConfigurationCreateOAuthConfig } from "../../funcs/oAuthConfigurationCreateOAuthConfig.js";
import { CreateOAuthConfigRequestRequest$zodSchema } from "../../models/createoauthconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateOAuthConfigRequestRequest$zodSchema,
};

export const tool$oAuthConfigurationCreateOAuthConfig: ToolDefinition<
  typeof args
> = {
  name: "O-auth-configuration-create-O-auth-config",
  description: `Create OAuth configuration

Create a new OAuth configuration for a connector type.<br><br>
<b>Admin Only:</b><br>
Only admins can create OAuth configurations. These provide the
OAuth credentials needed for users to authenticate connectors.<br><br>
<b>Use Case:</b><br>
Before users can create Google Drive connectors with OAuth,
an admin must create an OAuth configuration with the Google
OAuth client ID and secret.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await oAuthConfigurationCreateOAuthConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
