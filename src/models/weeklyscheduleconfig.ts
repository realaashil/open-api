/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const WeeklyScheduleConfigScheduleType = {
  Weekly: "weekly",
} as const;
export type WeeklyScheduleConfigScheduleType = ClosedEnum<
  typeof WeeklyScheduleConfigScheduleType
>;

export const WeeklyScheduleConfigScheduleType$zodSchema = z.enum([
  "weekly",
]);

/**
 * Run crawling job on specified days of the week
 */
export type WeeklyScheduleConfig = {
  scheduleType: WeeklyScheduleConfigScheduleType;
  isEnabled?: boolean | undefined;
  timezone?: string | undefined;
  daysOfWeek: Array<number>;
  hour: number;
  minute: number;
};

export const WeeklyScheduleConfig$zodSchema: z.ZodType<WeeklyScheduleConfig> = z
  .object({
    daysOfWeek: z.array(z.int()),
    hour: z.int(),
    isEnabled: z.boolean().default(true),
    minute: z.int(),
    scheduleType: WeeklyScheduleConfigScheduleType$zodSchema,
    timezone: z.string().default("UTC"),
  }).describe("Run crawling job on specified days of the week");
